import React from 'react';

function Aapp(props){
    return(
        <>
        <div>
        <h1>{props.title}</h1>
        <h1>{props.count}</h1>
        </div>
        </>
    );
    }
export default Aapp;